Added `--max-seconds` to [cartpole.py](/home/nick/Documents/cartpole-codex-add-max-seconds/cartpole.py#L34) and kept it optional with `default=None`, so `python cartpole.py` still follows the existing timestep-only stop condition. I also added a monotonic deadline in [cartpole.py](/home/nick/Documents/cartpole-codex-add-max-seconds/cartpole.py#L160) and check it at the top of the main training loop in [cartpole.py](/home/nick/Documents/cartpole-codex-add-max-seconds/cartpole.py#L164). When the deadline is reached, the loop `break`s normally, so shutdown still flows through the existing `finally` block.

Verification: `python3 -m py_compile cartpole.py` passed, and `python3 cartpole.py --help` shows the new `--max-seconds` flag. I also attempted an end-to-end smoke run with `--max-seconds`, but this environment blocks Gym’s async vector env setup with a `/dev/shm` permission error before training starts, so I could not fully validate the clean runtime exit here.